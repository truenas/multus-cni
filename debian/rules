#!/usr/bin/make -f
# export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_build:
	sh -c "\
		./build || exit 1; \
	"

override_dh_auto_install:
	sh -c "\
		mkdir -p debian/multus-cni/opt/cni; \
		cp -a bin debian/multus-cni/opt/cni/; \
	"

override_dh_shlibdeps:

override_dh_usrlocal:

override_dh_auto_clean:

override_dh_auto_test:
